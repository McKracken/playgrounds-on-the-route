[verified] No Feature 1 requirement quoted here specifies that a resolution-stage timeout returns `error: TIMEOUT`; AR-1.1 defines timeout bounds but not the resulting error classification, while FR-1.3 defines only `INVALID_INPUT` and `POI_NOT_FOUND`. Therefore, FR-5.1 references a TIMEOUT-during-resolution outcome that Feature 1 does not itself create, leaving a cross-feature specification gap.
